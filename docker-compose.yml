version: "3.9"

services:
  mapapp:
    build:
      context: ./MapApp
      dockerfile: Dockerfile
    container_name: voice_gis_app
    ports:
      - "5000:5000"
    volumes:
      - ./MapApp/uploads:/app/uploads
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
